DATABASE_URL="postgresql://postgres:postgres-password@localhost:5432/statuswatch"
JWT_SECRET="replace-with-a-random-hex-or-use-secrets-manager"
PORT=5555
# Optional API keys (leave blank or set via secrets manager)
RESEND_API_KEY=
# NEXT_PUBLIC_API_URL is used by the frontend when running locally
NEXT_PUBLIC_API_URL=http://localhost:5555

# Notes:
# - Replace placeholder values with secrets injected from your environment/CI provider.
# - Do NOT commit real secrets to git.# Database Configuration
DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/statuswatch

# Authentication
JWT_SECRET=your-super-secret-key-change-in-production

# Server Configuration
PORT=5555
NODE_ENV=development

# Email Configuration (Resend)
# Sign up at https://resend.com to get your API key
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=StatusWatch <notifications@yourdomain.com>

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Optional: For production
# NODE_ENV=production
# FRONTEND_URL=https://yourdomain.com
